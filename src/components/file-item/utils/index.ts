import { createDPF, createListOfPDF, createWord, fileSizeFormater, getExt, shrinkWord } from "./utils.files";
import {
    audioSelector,
    applicationSelector,
    checkIsCode,
    extensionSelector,
    fontSelector,
    getURLFileIco,
    imageSelector,
    mimeSelector,
    textSelector,
    videoSelector
} from "./mime.files"; import { readImage, resizeImage, readImagePromise } from "./reader.files";
export { createDPF, createListOfPDF, createWord, fileSizeFormater, getExt, shrinkWord };
export {
    audioSelector,
    applicationSelector,
    checkIsCode,
    extensionSelector,
    fontSelector,
    getURLFileIco,
    imageSelector,
    mimeSelector,
    textSelector,
    videoSelector
};

export {
    readImage, resizeImage, readImagePromise
}

